# PoW 仿真程序实验报告

## 1. 实验背景与目标

本项目依据课程要求，实现了一个使用 Go 编写的 PoW（Proof of Work）区块链仿真程序，用于在可控环境中观察链增长、网络传播和攻击行为。实验对应要求如下：

1. 模拟一定数量节点参与 PoW 出块。
2. 调整节点数量与每轮出块成功率，测量区块链增长速度。
3. 设置一定比例恶意节点，研究分叉攻击的成功概率。
4. 设置一定比例恶意节点，研究自私挖矿的收益比例。
5. 输出可复现实验结果，并据此完成分析报告。

本报告不仅给出实验结果，还完整解释程序的设计思路、各模块职责、关键设计理由、攻击仿真的具体实现方式，以及基于 [result.log](./result.log) 的完整数据分析。

## 2. 总体设计思路

### 2.1 设计目标

该仿真器的目标不是还原真实比特币协议的全部复杂细节，而是在保证可实现、可复现、可分析的前提下，抓住 PoW 系统的几个核心因素：

- 出块是概率事件。
- 节点之间存在传播延迟。
- 多节点可因信息不同步而产生竞争分叉。
- 恶意节点可以利用算力和传播时机改变主链结果。

因此，本项目采用“离散时间 + 中央调度网络 + 简化链状态”的仿真方式，在实现复杂度与实验解释性之间做平衡。

### 2.2 离散时间模型

系统用 Tick 作为基本时间单位，每一个 Tick 内发生以下四类事件：

1. 各节点按顺序尝试挖矿。
2. 新区块进入网络，网络为其分配传播延迟。
3. 到达当前 Tick 的消息被投递给目标节点。
4. 节点根据收件箱中的候选最高块更新本地链头。

这一流程由 [simulator.go](./simulator/simulator.go) 中的 `Simulator.Run()` 驱动。

### 2.3 为什么采用这种结构

选择这种结构有以下原因：

- **便于控制变量**：实验重点是节点数、难度、恶意比例等参数的影响，因此需要稳定、简单、可复现实验环境。
- **便于建模网络延迟**：用时间桶记录未来 Tick 的消息投递，既能模拟异步网络，又比真实线程通信更容易控制。
- **便于复现结果**：固定随机种子后，相同测试入口可得到相同结果。
- **便于观察攻击效果**：攻击逻辑只需作用于个别节点，不必实现完整的真实区块链协议栈。

## 3. 模块设计与实现细节

### 3.1 配置模块

[config.go](./config/config.go) 统一管理实验中的关键全局参数：

- `NodesCount`：节点总数，默认 `100`。
- `Difficulty`：单节点单 Tick 的基础出块概率系数，默认 `0.001`。
- `Target`：主链目标高度，默认 `1000`，性能实验和收益实验都以此为停止条件。
- `DelayRange`：网络传播延迟范围，默认 `3`，即消息可能在 `0,1,2` Tick 后送达。
- `RandSrc`：全局随机源，控制挖矿成功、网络延迟、分叉时的随机选链。
- `RandHashSrc`：生成伪哈希的随机源，用于构造区块标识。

#### 设计理由

- 把参数集中在配置模块，便于测试代码反复改写参数。
- `RandSrc` 与 `RandHashSrc` 分离，说明“出块过程的概率随机性”和“区块标识生成”在逻辑上是两件事，后者是固定随机种子的标识，并不影响实验逻辑。
- `Target=1000` 让实验既有足够长度，又不会把测试时间拉得过长。

### 3.2 区块与链结构

[block.go](./core/block.go) 中的 `Block` 结构体包含：

- `Tick`：区块被挖出的时刻。
- `Height`：区块高度。
- `MinerID`：出块节点编号。
- `Hash`：当前区块的伪哈希。
- `PreHash`：父区块哈希。

[blockchain.go](./core/blockchain.go) 中的 `Blockchain` 只保存：

- 创世块 `Genesis`
- 哈希到区块对象的映射 `Blocks`

#### 设计理由

- 项目不实现 UTXO、交易池、区块验证脚本等复杂逻辑，因为课程实验关注的是 PoW 竞争与链分叉。
- 用 `map[[32]byte]*Block` 保存全局区块，既能通过父哈希回溯链，又能方便统计不同链头。
- 节点自身不保存完整链，只由仿真器集中保存全局区块，降低内存开销，若保存，节点数为1000时内存开销直接来到10+GB，这不是本地单机可以做的。

### 3.3 节点模块

[node.go](./node/node.go) 中的 `Node` 是最重要的行为单元，核心字段包括：

- `ID`：节点编号。
- `Tip`：本地认为的链头。
- `HashPower`：节点算力，诚实节点默认是 `1`。
- `Inbox`：当前 Tick 收到的候选新区块集合。
- `Strategy`：节点策略，可为 `Honest`、`Fork`、`Selfish`。
- `ExpVar`：为实验攻击策略保存额外状态。

#### 3.3.1 挖矿过程

`Mine(tick)` 的成功概率是：

`P(success) = Difficulty * HashPower`

若成功出块，则新区块以前一个 `Tip` 为父块，并立即更新该节点自己的 `Tip`。这是合理的，因为矿工一旦自己出块，自然会继续在自己新出的块上挖矿。

#### 3.3.2 节点收块过程

`Receive(blk)` 并不会把所有收到的块都无条件接纳，而是只保留“当前已知的最高块集合”：

- 如果 `Inbox` 为空，则只接收高度高于当前 `Tip` 的块。
- 如果 `Inbox` 非空，则只保留高度不低于当前 `Inbox` 最高高度的块。
- 如果出现更高的块，会清空旧的候选集合，只留下新的最高块集合。

#### 3.3.3 节点更新链头

`UpdateTip()` 会从 `Inbox` 中随机选择一个最高块作为新的 `Tip`。这样做模拟了真实网络中“多个同高分叉到达顺序不同、节点观察不同”的情况。

#### 3.3.4 节点不保存完整本地区块链

源码中明确注释掉了节点本地 `KnownBlocks` 结构，这是一项有意识的简化设计：

- 本项目的实验指标主要依赖全局主链高度与链头集合。
- 把完整链状态集中交给仿真器保存，可以显著减少重复状态。
- 对本实验中的攻击统计而言，节点只需知道当前 `Tip` 与收到的最高块即可。
- 若保存，内存开销时不可接受的，可以自行尝试取消注释运行

### 3.4 网络模块

[network.go](./network/network.go) 中的 `Network` 负责模拟广播延迟。其核心设计是一个按 Tick 分桶的消息表：

- `Packets[tick]` 表示将在第 `tick` 轮投递的所有消息。

当新区块进入网络时，`ProcessPacket()` 会对每个目标节点独立生成一个随机延迟 `0 <= delay < DelayRange`，并把消息加入 `Packets[currentTick + delay]`。

随后 `SendPackets(tick)` 把当前 Tick 到期的所有消息投递给对应节点。

#### 设计理由

- 这种时间桶设计非常适合离散事件仿真。
- 与直接线程睡眠或真实并发网络相比，它更容易复现、更容易分析。
- 每个目标节点具有不同延迟，可以自然制造信息不一致，从而形成分叉和竞争。

### 3.5 仿真器模块

[simulator.go](./simulator/simulator.go) 中的 `Simulator` 聚合了：

- 网络对象 `Network`
- 全局区块集合 `Chain`
- 当前所有最高链头 `Tips`
- 全局时钟 `Tick`

#### 3.5.1 单轮执行流程

`Run()` 每轮执行步骤如下：

1. `Tick++`
2. 所有节点依次调用 `Mine()`
3. 若产生新区块，则写入全局链表 `Chain.Blocks`
4. 更新仿真器维护的最高链头集合 `Tips`
5. 将新区块交给网络层处理传播
6. 网络投递当前 Tick 到期的消息
7. 若存在自私挖矿节点，则处理其待发布私有块
8. 所有节点调用 `UpdateTip()` 完成状态更新
9. 当最高链高度达到 `Target` 时结束

#### 3.5.2 为什么挖矿是顺序执行而非并发执行

源码注释已经明确说明，本人尝试过并发仿真，但结果不理想，因此采用顺序执行。这样做的理由是：

- 并发会引入额外调度不确定性，不利于复现实验。
- 本项目的网络异步性已经由传播延迟模型体现，不一定要再叠加线程级竞争。
- 实验重点是链增长规律和攻击效果，而不是系统级并发性能。
- 在并发的尝试中，在每个routine中使用sleep会显著拖慢实验进度且不如tick更好管理；如果不适用sleep由channel信号唤醒节点则会因为网络广播routine的速度显著低于节点计算速度导致几乎时所有节点在独立挖矿，而消息堵死在channel缓冲区，网络和模拟器的channel缓冲区直接爆炸导致死锁。

### 3.6 实验模块

#### 3.6.1 参数实验入口

[main_test.go](./main_test.go) 的 `TestParameters()` 枚举不同：

- 节点数 `NodesCount`
- 难度 `Difficulty`

然后运行仿真并输出：

`Block Speed = sim.Tick / Target`

这里的单位是 `ticks/block`，数值越小代表出块越快。

#### 3.6.2 分叉攻击实验

[fork_attack.go](./exp/fork_attack.go) 中的 `ForkAttack()` 做了三件事：

1. 指定恶意节点为 `sim.Network.Nodes[0]`
2. 按目标恶意算力占比，计算其 `HashPower`
3. 重复仿真 `10000` 轮，统计成功次数

恶意节点算力计算公式为：

若诚实节点共有 $N-1$ 个，且每个算力为 `1`，恶意节点算力记为 $H_m$，目标恶意算力占比为 $r$，则：

$$H_m / (H_m + N - 1) = r$$

整理得：

$$H_m = (N - 1) * r / (1 - r)$$

这正是 `CalculateNodePower()` 的实现逻辑。

#### 3.6.3 分叉攻击的实现机制

分叉攻击节点使用 `Strategy=Fork`。在 `Mine()` 中：

- 恶意节点会持续在自己的私有链上挖矿。
- 在到达目标高度之前，不向外广播新区块。
- 当恶意私有链区块高度达到 `7` 时，才返回该区块并触发成功判定流程。

随后，仿真器检查当前公共最高链头是否仍不超过高度 `6`：

- 若是，则说明恶意节点先于公共链完成目标长度私有链，攻击成功。
- 否则攻击失败。

换言之，这里研究的是“能否提前完成一个 6 长度的私有分叉并具备替换主链的条件”。

#### 3.6.4 自私挖矿实验

[selfish_mining.go](./exp/selfish_mining.go) 中的 `Selfish()` 会重复仿真 `1000` 轮。恶意节点同样被设置为 `Nodes[0]`，并使用前述算力公式设置 `HashPower`。

其机制如下：

- 恶意节点挖到块后，不立即广播，而是追加到 `PrivateChain`。
- 当恶意节点收到外部新区块时，如果公共链已经追上其私有链前部高度，则把对应私有块加入 `Release` 队列。
- 仿真器在每轮内把 `Release` 中的块逐个注入网络，形成延迟发布行为。

最后，程序从仿真结束时的所有最高链头回溯统计主链中由恶意节点挖出的区块数，并计算：

`Profitability = 恶意区块数 / Target`

若存在多个最高链头，则代码使用平均方式处理：

`selfishIncome += sum / len(sim.Tips)`

这使结果反映的是“最终主链上恶意区块占比”的平均水平。

## 4. 关键设计理由与模型假设

### 4.1 采用中央全局链，而不是节点各自维护完整链

这样做牺牲了一部分真实性，但换来更低的状态复杂度。实验所需的核心指标只有：

- 当前主链高度
- 竞争链头数量
- 恶意块在最终链中的占比

因此，全局集中记录即可满足需求。

### 4.2 用随机延迟模拟网络，而不是显式建立节点拓扑

本实验重点不是研究具体网络拓扑，而是研究“传播不一致”对分叉和攻击的影响。用随机传播延迟即可抓住关键因素。

### 4.3 出块按 Tick 进行，而不是连续时间泊松过程

严格来说，真实 PoW 更接近连续时间随机过程；本项目把它离散化为每 Tick 一次 Bernoulli 试验。这种简化有两个优势：

- 更容易实现与调试。
- 更适合在程序中逐轮注入网络延迟和策略行为。

### 4.4 模型中的简化之处

相较真实区块链系统，本模型没有实现：

- 动态难度调整
- 真实哈希计算
- 交易池与交易验证
- 完整的最长链 / 最重链协议细节
- 复杂自私挖矿状态机
- 带宽瓶颈与节点拓扑结构

因此，实验结果反映的是“该简化模型下的趋势”，而不是对真实公链参数的直接预测。

## 5. 实验设置与可复现性

### 5.1 固定参数

除特定实验中显式修改的量之外，本项目默认：

- `Target = 1000`
- `DelayRange = 3`
- 诚实节点单个算力为 `1`

### 5.2 随机种子

项目中使用了固定或可控随机种子：

- [config.go](./config/config.go) 中 `RandSrc = PCG(42, 42)`
- [config.go](./config/config.go) 中 `RandHashSrc = ChaCha8([32]byte{4,2})`
- [fork_attack_test.go](./exp/fork_attack_test.go) 中使用 `randSeed1 = {7, 21}`
- [selfish_mining_test.go](./exp/selfish_mining_test.go) 中使用 `randSeed2 = {7, 21}`

攻击实验中，每一轮会用给定生成器继续派生新的 PCG 随机源，因此在相同测试顺序下可以复现相同输出。

### 5.3 结果来源

本报告中的全部数值均来自 [result.log](./result.log)，即：

```bash
go test -v ./...
```

的输出整理结果。

## 6. 实验数据与完整分析

### 6.1 参数实验原始数据整理

#### 6.1.1 节点数为 10

| Difficulty | Ticks/Block |
| --- | ---: |
| 0.0001 | 968.880 |
| 0.0005 | 206.761 |
| 0.0010 | 102.275 |
| 0.0050 | 21.115 |
| 0.0100 | 11.531 |
| 0.0200 | 6.519 |
| 0.0500 | 3.045 |
| 0.1000 | 2.074 |
| 0.2000 | 1.377 |
| 0.2500 | 1.245 |
| 0.3000 | 1.148 |
| 0.5000 | 1.006 |

#### 6.1.2 节点数为 50

| Difficulty | Ticks/Block |
| --- | ---: |
| 0.0001 | 206.231 |
| 0.0005 | 40.833 |
| 0.0010 | 21.944 |
| 0.0050 | 5.293 |
| 0.0100 | 3.253 |
| 0.0200 | 2.106 |
| 0.0500 | 1.319 |
| 0.1000 | 1.044 |
| 0.2000 | 1.000 |

#### 6.1.3 节点数为 100

| Difficulty | Ticks/Block |
| --- | ---: |
| 0.0001 | 102.201 |
| 0.0005 | 21.927 |
| 0.0010 | 12.083 |
| 0.0050 | 3.124 |
| 0.0100 | 2.177 |
| 0.0200 | 1.513 |
| 0.0500 | 1.043 |
| 0.1000 | 1.000 |

#### 6.1.4 节点数为 200

| Difficulty | Ticks/Block |
| --- | ---: |
| 0.0001 | 51.333 |
| 0.0005 | 11.440 |
| 0.0010 | 6.215 |
| 0.0050 | 2.065 |
| 0.0100 | 1.475 |
| 0.0200 | 1.114 |
| 0.0500 | 1.000 |

#### 6.1.5 节点数为 500

| Difficulty | Ticks/Block |
| --- | ---: |
| 0.0001 | 21.692 |
| 0.0005 | 5.704 |
| 0.0010 | 3.247 |
| 0.0050 | 1.336 |
| 0.0100 | 1.036 |
| 0.0200 | 1.000 |

#### 6.1.6 节点数为 1000

| Difficulty | Ticks/Block |
| --- | ---: |
| 0.0001 | 11.613 |
| 0.0005 | 3.321 |
| 0.0010 | 2.137 |
| 0.0050 | 1.056 |
| 0.0100 | 1.000 |

### 6.2 参数实验规律分析

#### 6.2.1 总出块强度决定主要趋势

若记系统总出块强度近似为：

`lambda = NodesCount * Difficulty`

则实验结果说明，`Ticks/Block` 主要由 `lambda` 决定。下面列出若干“不同节点数但相同 lambda”的数据对比：

| lambda | 参数组合 | Ticks/Block |
| --- | --- | ---: |
| 0.01 | (10, 0.001) | 102.275 |
| 0.01 | (100, 0.0001) | 102.201 |
| 0.05 | (10, 0.005) | 21.115 |
| 0.05 | (50, 0.001) | 21.944 |
| 0.05 | (500, 0.0001) | 21.692 |
| 0.10 | (10, 0.01) | 11.531 |
| 0.10 | (100, 0.001) | 12.083 |
| 0.10 | (200, 0.0005) | 11.440 |
| 0.10 | (1000, 0.0001) | 11.613 |
| 0.50 | (10, 0.05) | 3.045 |
| 0.50 | (50, 0.01) | 3.253 |
| 0.50 | (100, 0.005) | 3.124 |
| 0.50 | (500, 0.001) | 3.247 |
| 1.00 | (10, 0.1) | 2.074 |
| 1.00 | (50, 0.02) | 2.106 |
| 1.00 | (100, 0.01) | 2.177 |
| 1.00 | (200, 0.005) | 2.065 |
| 1.00 | (1000, 0.001) | 2.137 |

这些数据非常接近，说明在本仿真模型中，系统总成功概率强度是控制链增长速度的主导因素，而不是节点数量单独起作用。

#### 6.2.2 低强度区间近似符合反比关系

当 `lambda` 较小，例如 `0.01`、`0.05`、`0.1` 时，`Ticks/Block` 分别大约是：

- `lambda=0.01` 时约 `102`
- `lambda=0.05` 时约 `21`
- `lambda=0.1` 时约 `12`

这与“总出块成功率越大，平均等待时间越短”的直觉完全一致，近似呈现反比关系。

#### 6.2.3 高强度区间出现饱和

当 `lambda` 增大到 `1` 甚至更高时，`Ticks/Block` 开始向 `1` 靠近。例如：

- `NodeCount=1000, Difficulty=0.01` 时结果是 `1.000`
- `NodeCount=500, Difficulty=0.02` 时结果是 `1.000`
- `NodeCount=200, Difficulty=0.05` 时结果是 `1.000`

原因是仿真器采用离散 Tick 模型，每轮最多只会让链高度增长有限步数，因此平均值存在明显下界，无法无限逼近 0。

#### 6.2.4 实测值通常略大于理想反比值

理论上，若完全忽略传播冲突和链竞争，系统出块时间近似和 `1 / lambda` 成正比。但在实验中，实测值通常略高于这一理想值，例如：

- `lambda=0.1` 的理想值大约为 `10`，实测在 `11.4` 到 `12.1` 之间
- `lambda=0.5` 的理想值大约为 `2`，实测在 `3.0` 到 `3.25` 之间
- `lambda=1.0` 的理想值大约为 `1`，实测在 `2.06` 到 `2.18` 之间

这说明网络延迟和同高竞争分叉让部分出块没有立即转化为主链增长，从而拉大了平均 `ticks/block`。

### 6.3 分叉攻击实验结果与分析

#### 6.3.1 原始结果

| 恶意比例 | 100 节点, Diff=0.001 | 50 节点, Diff=0.001 | 100 节点, Diff=0.0005 |
| --- | ---: | ---: | ---: |
| 0.4 | 0.298300 | 0.268400 | 0.271300 |
| 0.3 | 0.082300 | 0.075200 | 0.075600 |
| 0.2 | 0.011000 | 0.007700 | 0.007800 |
| 0.1 | 0.000300 | 0.000300 | 0.000000 |

#### 6.3.2 恶意算力比例是最敏感因素

无论节点数和难度如何变化，恶意比例从 `0.1` 提高到 `0.4` 后，分叉成功率都出现数量级上升：

- 在 `100 节点, Diff=0.001` 下，从 `0.03%` 上升到 `29.83%`
- 在 `50 节点, Diff=0.001` 下，从 `0.03%` 上升到 `26.84%`
- 在 `100 节点, Diff=0.0005` 下，从 `0%` 上升到 `27.13%`

这说明算力集中度是决定攻击可行性的核心因素。

#### 6.3.3 节点数变化的影响相对较小

比较：

- `100 节点, Diff=0.001`
- `50 节点, Diff=0.001`

可以看到两组结果非常接近。例如在恶意比例 `0.4` 时仅相差约 `2.99` 个百分点。说明在本模型中，只要恶意算力比例相同，单纯减少或增加节点数并不会改变结论的方向，更多体现为细微扰动。

#### 6.3.4 较低难度会略微降低攻击成功率

比较：

- `100 节点, Diff=0.001`
- `100 节点, Diff=0.0005`

可见各恶意比例下，较低难度时成功率略低。这说明当全网出块更慢时，固定传播延迟在相对时间尺度上的影响减弱，诚实网络更容易维持一致视图，从而让私有分叉抢跑的优势变小。

### 6.4 自私挖矿实验结果与分析

#### 6.4.1 原始结果

| 恶意比例 | 100 节点, Diff=0.001 | 50 节点, Diff=0.001 | 100 节点, Diff=0.0005 |
| --- | ---: | ---: | ---: |
| 0.4 | 0.550945 | 0.496300 | 0.497569 |
| 0.3 | 0.240290 | 0.222360 | 0.225706 |
| 0.2 | 0.103534 | 0.097537 | 0.097984 |
| 0.1 | 0.036417 | 0.034672 | 0.035022 |

#### 6.4.2 高恶意比例下存在明显收益放大

当恶意比例为 `0.4` 时：

- `100 节点, Diff=0.001` 的收益占比达到 `0.550945`
- `50 节点, Diff=0.001` 的收益占比达到 `0.496300`
- `100 节点, Diff=0.0005` 的收益占比达到 `0.497569`

也就是说，攻击者拥有 `40%` 左右算力时，最终却能拿到接近 `50%` 到 `55%` 的主链收益，已经明显超过其名义算力占比。这正是自私挖矿的核心危害。

#### 6.4.3 低恶意比例下，收益放大不明显

当恶意比例只有 `0.1` 时，收益只有约 `3.5%` 左右，显著低于其 `10%` 的名义算力占比。这说明在本模型中，低算力攻击者长期隐匿区块的代价高于潜在收益，难以通过延迟发布形成稳定优势。

#### 6.4.4 难度和节点数的影响方向与分叉攻击一致

与分叉攻击相似：

- 相同难度下，`100` 节点的收益放大略强于 `50` 节点。
- 相同节点数下，较高难度 `0.001` 比较低难度 `0.0005` 更有利于攻击者。

原因仍然是：当全网出块更频繁时，传播延迟带来的局部信息不一致更容易被恶意节点利用。

## 7. 结果解释与课程要求对应

### 7.1 关于“节点数量、成功率与链增长速度”

实验结果清楚表明：

- 节点总数越多，在相同单节点成功率下，全网总出块强度越高，链增长越快。
- 单节点成功率越高，在相同节点数下，链增长越快。
- 真正起主导作用的是系统总出块强度 `NodesCount * Difficulty`。
- 当总出块强度足够高时，离散时间模型会出现每 Tick 接近必出块的饱和现象。

### 7.2 关于“不同恶意节点比例下，分叉攻击成功率”

实验给出了从 `10%` 到 `40%` 的完整对比：

- `10%` 基本无法成功
- `20%` 成功率仍然很低
- `30%` 开始进入可观测风险区间
- `40%` 已经达到约 `27%` 到 `30%` 的高风险水平

这表明分叉攻击对算力比例极为敏感。

### 7.3 关于“不同恶意节点比例下，自私挖矿收益比例”

实验结果说明：

- 恶意比例较低时，自私挖矿收益低于其算力份额
- 恶意比例提高后，收益份额会超过算力份额
- 在 `40%` 恶意比例时，收益提升已经非常明显

因此，自私挖矿不是简单的“占多少算力拿多少收益”，而是可能形成超线性收益。

## 8. 模型局限性

虽然本项目已经较好完成课程要求，但仍有以下局限：

1. 网络模型只使用均匀随机延迟，没有真实拓扑和带宽限制。
2. 没有实现真实 PoW 哈希与难度压缩表示。
3. 自私挖矿策略是简化版，并非完整论文级状态机。
4. 分叉攻击的成功判定以固定目标长度为准，而不是完整重组概率模型。
5. 节点不维护完整本地区块树，因此某些复杂行为无法表达。

这些局限不会影响课程实验目标，但应在解释结果时明确说明。

## 9. 结论

本项目成功实现了一个可复现的 PoW 仿真平台，并完整完成了课程要求中的三类实验任务。综合源码实现与实验数据，可以得到以下更完整的结论：

1. **系统性能方面**：区块链增长速度主要由 `NodesCount * Difficulty` 决定，说明系统总算力强度是链增长的第一控制因素。
2. **网络影响方面**：即使不实现复杂网络拓扑，仅传播延迟和随机到达顺序就足以制造显著分叉与链竞争。
3. **安全性方面**：当恶意算力比例从 `10%` 增长到 `40%` 时，分叉攻击成功率出现数量级跃升，表明 PoW 对高算力攻击非常敏感。
4. **经济激励方面**：自私挖矿在高恶意比例下可以获得高于自身算力份额的收益，说明不诚实策略可能破坏系统原有激励相容性。
5. **实验方法方面**：采用离散 Tick、中央网络与固定随机种子的设计，使实验具有良好的可解释性和可复现性，适合作为教学与课程项目中的仿真平台。

总体来看，该仿真器虽然是简化模型，但已经清楚地展示了 PoW 系统中的三个核心事实：链增长依赖总算力、传播延迟会造成分叉、算力集中会同时放大安全风险与攻击收益。这些现象与区块链共识理论中的基本认识是一致的。
